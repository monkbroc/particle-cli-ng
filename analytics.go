package main

import "fmt"

var analyticsTopic = &Topic{
	Name:   "analytics",
	Hidden: true,
}

var analyticsSubmitCmd = &Command{
	Topic:   "analytics",
	Command: "submit",
	Hidden:  true,
	Run: func(ctx *Context) {
		ioutil.ReadFile(Cmd
		json.Unmarshal()
		fmt.Println("S")
	},
}
